{
	"name": "commity-ai",
	"displayName": "Commity.ai",
	"description": "Lightning-fast AI commit messages. Smart, instant, and context-aware.",
	"version": "2.0.0",
	"publisher": "ryanlabs",
	"author": "mickcodez",
	"license": "SEE LICENSE IN LICENSE",
	"repository": {
		"type": "git",
		"url": "https://github.com/mickr/commity.git"
	},
	"bugs": {
		"url": "https://github.com/mickr/commity/issues"
	},
	"icon": "media/icons/commity-logo.png",
	"homepage": "https://commity.ai",
	"keywords": [
		"git",
		"commit",
		"ai",
		"commit-message",
		"scm"
	],
	"engines": {
		"vscode": "^1.84.0"
	},
	"categories": [
		"SCM Providers"
	],
	"activationEvents": [],
	"main": "./out/extension.js",
	"extensionDependencies": [
		"vscode.git"
	],
	"contributes": {
		"commands": [
			{
				"command": "commity.generateCommitMessage",
				"title": "Generate Commity Message",
				"icon": {
					"light": "media/icons/commity-logo-light.svg",
					"dark": "media/icons/commity-logo-dark.svg"
				}
			},
			{
				"command": "commity.refreshReflog",
				"title": "Refresh Reflog",
				"icon": "$(refresh)"
			},
			{
				"command": "commity.reflog.focusUp",
				"title": "Focus Up",
				"enablement": "view == commity.reflogView"
			},
			{
				"command": "commity.reflog.focusDown",
				"title": "Focus Down",
				"enablement": "view == commity.reflogView"
			},
			{
				"command": "commity.reflog.select",
				"title": "Select Entry",
				"enablement": "view == commity.reflogView"
			}
		],
		"keybindings": [
			{
				"command": "commity.reflog.focusUp",
				"key": "up",
				"when": "view == commity.reflogView && viewVisible == commity.reflogView"
			},
			{
				"command": "commity.reflog.focusDown",
				"key": "down",
				"when": "view == commity.reflogView && viewVisible == commity.reflogView"
			},
			{
				"command": "commity.reflog.select",
				"key": "space",
				"when": "view == commity.reflogView && viewVisible == commity.reflogView"
			},
			{
				"command": "commity.reflog.select",
				"key": "enter",
				"when": "view == commity.reflogView && viewVisible == commity.reflogView"
			}
		],
		"menus": {
			"scm/title": [
				{
					"command": "commity.generateCommitMessage",
					"group": "navigation",
					"when": "scmProvider == git"
				}
			],
			"view/title": [
				{
					"command": "commity.refreshReflog",
					"when": "view == commity.reflogView",
					"group": "navigation"
				}
			]
		},
		"viewsContainers": {
			"activitybar": [
				{
					"id": "commity",
					"title": "Commity",
					"icon": "media/icons/commity-logo-dark.svg"
				}
			]
		},
		"views": {
			"commity": [
				{
					"id": "commity.reflogView",
					"name": "Reflog",
					"type": "webview"
				}
			]
		}
	},
	"scripts": {
		"vscode:prepublish": "npm run package",
		"compile": "npm run check-types && node esbuild.js",
		"watch": "npm-run-all -p watch:*",
		"watch:esbuild": "node esbuild.js --watch",
		"watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
		"watch:tsc-webview": "tsc --noEmit --watch --project src/webview/tsconfig.json",
		"package": "npm run check-types && node esbuild.js --production",
		"check-types": "tsc --noEmit",
		"pretest": "npm run compile && npm run lint",
		"lint": "eslint src --ext ts",
		"test": "jest",
		"test:watch": "jest --watch",
		"test:coverage": "jest --coverage",
		"version:patch": "npm version patch --no-git-tag-version",
		"version:minor": "npm version minor --no-git-tag-version",
		"version:major": "npm version major --no-git-tag-version",
		"build:patch": "npm run version:patch && npm run package && vsce package --no-dependencies",
		"build:minor": "npm run version:minor && npm run package && vsce package --no-dependencies",
		"build:major": "npm run version:major && npm run package && vsce package --no-dependencies"
	},
	"devDependencies": {
		"@semantic-release/changelog": "^6.0.3",
		"@semantic-release/exec": "^6.0.3",
		"@semantic-release/git": "^10.0.1",
		"@types/jest": "^30.0.0",
		"@types/js-yaml": "^4.0.9",
		"@types/node": "20.x",
		"@types/react": "^19.2.5",
		"@types/react-dom": "^19.2.3",
		"@types/vscode": "^1.80.0",
		"@typescript-eslint/eslint-plugin": "^6.4.1",
		"@typescript-eslint/parser": "^6.4.1",
		"@vscode/vsce": "^3.2.1",
		"dotenv": "^17.2.3",
		"esbuild": "^0.25.11",
		"eslint": "^8.47.0",
		"jest": "^30.2.0",
		"js-yaml": "^4.1.0",
		"npm-run-all": "^4.1.5",
		"openai": "^4.75.0",
		"prettier": "^3.6.2",
		"react": "^19.2.0",
		"react-dom": "^19.2.0",
		"semantic-release": "^24.2.0",
		"ts-jest": "^29.4.5",
		"typescript": "^5.1.6",
		"zod": "^3.23.8"
	},
	"dependencies": {
		"@vscode/codicons": "^0.0.43",
		"isomorphic-git": "^1.35.1"
	}
}
